# Rule: context7 mcp

**Before writing code, look the API up in the `Context7` MCP** — it serves
  current package docs, so you match our pinned version instead of a remembered one
  (`pyproject.toml` wins if they disagree). Skip for our own code, stdlib, and docs.
