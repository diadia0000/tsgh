# 效能瓶頸清單 — 混合管線（實測）

> 配套文件：`perf_report.html`。執行方案 §5.2/§5.3（候選項逐個記錄 + Amdahl 停損）及 §6（分類只含現象描述，**無修復建議**）。
> 實測環境：git `96a28ba`、config_hash `db2b7e6a`、RTX 5090 / CUDA 13.0 / torch 2.10.0+cu130，{小 25 · 中 121 · 大 441} 地磚真實 WSI 裁切。
> 下列數值主要針對 **大（441 地磚）** 基準 = **848.0 秒**。

## 基準點（控制組 / 「樸素版本」 基線 — 保留，勿覆蓋）

| 規模 | 地磚數 | 網格 | 端到端 | 秒/地磚 | 預切 A | 細胞數 | 背景地磚 | 峰值 RSS | 峰值 VRAM |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 小  | 25  | 5×5   | 54.3 秒  | 2.17 | 1.45 秒  | 22  | 3  | 2.82 GB | 4.68 GB |
| 中 | 121 | 11×11 | 243.3 秒 | 2.01 | 5.57 秒  | 103 | 18 | 3.07 GB | 4.68 GB |
| 大  | 441 | 21×21 | 848.0 秒 | 1.92 | 20.58 秒 | 379 | 62 | 4.04 GB | 4.68 GB |

完整 WSI（35,700 地磚 @ 1024px）線性外推：`總耗時 ≈ 9.4 秒 + 1.903 秒/地磚`
⇒ **~18.9 小時**，視為 **上界**（裁切的組織密度 ~85%；真實玻片多為白色背景，空核心地磚執行快速路徑）。

## Amdahl 停損（方案 §5.2）

排序依據為 **基準的百分比，而非絕對秒數**。項目 **< ~10%**（Amdahl 天花板 < 1.11）已記錄但 **本輪未深入分析**。深入分析的候選項因此只有 #1–#3；#4–#7 為完整性記錄。

---

## 深入分析候選項（貢獻度不可忽略）

### ① GPU 未充分利用 — 源自完全串列管線 — **首要問題**
| 欄位 | 說明 |
|---|---|
| 現象 | GPU 閒置 **~46–49%** 時間（大規模：idle_frac 0.459），平均 SM 使用率 **~28%**，即使是唯一的 GPU 工作。B1 前向傳播 = **牆鐘時間的 45.5%** = 三個串列前向傳播/地磚（M2 Cellpose + M3b Cellpose + M1 UNet）共享一個 CUDA 上下文。VRAM 峰值 **5.16 / 32 GB**（batch_size 硬編碼 16）。|
| 佔總時間 | **45.5%**（B1 階段）；閒置本身 ≈ 牆鐘時間的一半 |
| Amdahl 天花板 | 1.84（B1 階段）— 但真正的槓桿是閒置，而非 B1 運算 |
| 所在階段 | B1（× 跨層 §4.1/§4.7） |
| 量測規模 | 25/121/441 地磚 — 區間穩定（閒置 0.49→0.48→0.46） |
| 新發現 | 舊 03/04 文件 *懷疑* GPU 飢餓但未驗證；**今已實測** |
| 分類方向 | **Class 3（平行/並行）+ Class 6（架構）**：串列 `run_batch` 迴圈、無跨地磚 GPU 批處理、模型從未與 CPU 工作重疊 |
| 信心度 | 實測直接量測（measured directly） |

### ② `detect_all_dots` — M3 HER2/CEP17 點檢測
| 欄位 | 說明 |
|---|---|
| 現象 | **260.1 秒 = 30.7%** 牆鐘時間 — 單一最大的子項目，超過任何一次 GPU 前向傳播。純 CPU LAB + H-形態學點檢測（per cell），透過 joblib `n_jobs=-1` 派遣；主執行緒阻塞在子流程工作者（cProfile 主執行緒顯示 ~11.5 秒 `time.sleep`）。隨 **細胞數** 增長（121 地磚時 26.4% → 更密集的 441 地磚裁切時 30.7%）。|
| 佔總時間 | **30.7%** |
| Amdahl 天花板 | 1.44 |
| 所在階段 | B3 |
| 量測規模 | 121/441 地磚；隨組織/細胞密度縮放 |
| 新發現 | 舊 03 文件記錄為 17.1%；**現更高**（去重複重構 + 輸入更密集） |
| 分類方向 | **Class 1（演算法複雜度）**+ Class 3（已是 CPU 平行，但於 GPU 閒置時執行） |
| 信心度 | 實測直接量測 |

### ③ PNG 編碼 + 每地磚產物的寫入
| 欄位 | 說明 |
|---|---|
| 現象 | **76.8 秒 = 9.05%** 牆鐘時間。每個地磚無條件寫入 `core_mask` / `masked_ihc` / `dish_mask_overlay` 為無損 PNG 格式（透過 `skimage.io.imsave`）。（int32 TIFF 標籤寫入另計 < 0.5%；per-cell 裁切 ~0.5%。） |
| 佔總時間 | **9.05%** |
| Amdahl 天花板 | 1.10 |
| 所在階段 | B2 |
| 量測規模 | 25/121/441 — 穩定 ~9–10% |
| 新發現 | 新的分解；舊文件將「13.4% 檔案 I/O」歸類不同，且早於 int32-TIFF 引入 |
| 分類方向 | **Class 5（I/O & 儲存配置）**：關鍵路徑上的無損 PNG 編碼（除錯陣列） |
| 信心度 | 實測直接量測 |

---

## Amdahl 下限以下的項目（已記錄但本輪未深入分析 — 方案 §5.2）

### ④ 每地磚 `gc.collect()` — 地磚邊界清理
- **36.3 秒 = 4.28%**，天花板 1.04。完全由 `run_batch` 中每地磚執行一次的顯式 `gc.collect()`（`torch.cuda.empty_cache` 另計 < 0.3%）。每地磚進行一次完整 Python 垃圾回收掃描是固定開銷，與地磚數線性相關。**從未量測過**。Class 4（記憶體生命週期）+ 6（框架）。信心度：實測。

### ⑤ 階段 A 預切（2.43%）& 階段 D 疊合拼接（0.60%）— 新增階段
- 兩個全新階段是舊效能報告從未涵蓋的。預切 A ~2.4%（每個規模都是，真實 8 執行緒平行 I/O，pyvips 釋放 GIL）。拼接 D ~0.6% — 所有分析後單次串列 `pyvips` join+lzw+`tiffsave`（讀取+join+壓縮在一個 C 呼叫中融合，不可由 Python 計時分離）。在完整 WSI 時，D 成為單次串列約數分鐘的塊，但仍 < ~19 小時執行的 1%。Class 5（I/O）；D 亦 Class 3（串列，可與分析重疊）。信心度：實測 + 外推。

### ⑥ 模型初始化（一次性）
- 25 地磚時 7.3% → 121 時 1.3% → **441 時 0.37%**。純一次性載入，在 WSI 規模時攤銷至可忽略。Class 6。資訊性。信心度：實測。

### ⑦ API / 工作層（階段 E）
- `submit_job` 入隊 **2.3 µs** 平均；BackgroundTask 派遣 **~3.8 ms** — ~10⁻⁷ 多小時執行的時間。可忽略。唯一注意事項：並行分析請求各佔用一個執行緒池工作者，但在單一 GPU/CUDA 上下文上序列化（未來多請求風險，非當前熱點）。Class 6。信心度：實測。

---

## 記憶體成長聲明驗證（方案 §3.6 / §4.3）
- **VRAM**：所有規模下平坦 **5.16 GB / 32 GB** — **不** 隨地磚數增長。✔ 有界。
- **RSS**：2.82 → 3.07 → 4.04 GB（跨越 25 → 121 → 441 地磚，17.6× 地磚，+43% RSS）— 顯然 **對地磚數次線性**，但非完全平坦：追蹤 **總累積細胞結果**（`per_tile_owned` 存於 RAM 直到最終全域合併），即隨細胞數增長而非地磚數。新架構「記憶體有界，不隨地磚線性」的聲明 **成立**；殘差成長由細胞數驅動，非已刪除的 400 GB 畫布。

## 分類摘要（方案 §6）
| 類別 | 瓶頸 |
|---|---|
| 1 演算法/模型複雜度 | ② detect_all_dots；① 內的 Cellpose SAM `get_rel_pos` 前向傳播成本 |
| 3 平行/並行 | ① 串列管線 / GPU 閒置；D 串列拼接 |
| 4 記憶體生命週期 | ④ 每地磚 gc；RSS 細胞結果累積 |
| 5 I/O & 儲存 | ③ PNG 編碼；⑤ 預切 & 拼接 |
| 6 架構/框架 | ① 無跨地磚批處理；⑥ 初始化；⑦ API 層 |
| 7 配置/無效程式碼 | `cellpose_batch_size` 無效（G-B）— 必須在任何 batch-size 掃描前修正 |

---

## 下一階段（解決方案設計，超出本檔範圍）

依據方案 §5.2/§6，本文件僅作分類。對 **①（首要）** 的解決方案設計追蹤位於 [../10-gpu-serial-pipeline-plan.md](../10-gpu-serial-pipeline-plan.md)。該文件也說明為何本輪不獨立處理 ② 和 ③（其牆鐘成本幾乎完全重疊於 ① 的閒置時間，若實施 ① 的修正，② 和 ③ 可能也會一併被解決）。
