# Discovered but not implemented — full audit of docs 01–25 + measurement/*.md

> Compiled 2026-07-26, after reading every document in `docs/hybrid-pipeline/` (01–25, the two
> playbook docs, README, and every file in `measurement/`, excluding the `-zh` translations and the
> HTML files). **Purpose**: a single place listing every idea, candidate, or finding that this
> project's own Discover/Analyze/Plan work surfaced but that never resulted in a pipeline code
> change — whether it is still open, was explicitly rejected after measurement (stop-lossed), or is
> blocked on something else. This is a one-time compiled audit, not a living backlog — for the
> ranked, actively-maintained "what's left" list, use [`19-open-backlog.md`](./19-open-backlog.md)
> and [`measurement/bottleneck-list.md`](./measurement/bottleneck-list.md), which this doc
> cross-references rather than duplicates. Update this file only if you do a similarly full re-audit;
> for day-to-day tracking, 19 is the authority.
>
> **Reading key** — every row is one of:
> - 🟢 **Open / actionable** — identified, not built, not rejected. Real backlog.
> - 🟡 **Gated / blocked** — built or ready, but waiting on another item (validation, a decision, an
>   environment fix) before it can ship or be attempted.
> - 🔴 **Stop-lossed** — measured (or reasoned) and explicitly rejected. Recorded so nobody re-proposes
>   it without new evidence.
> - ⚪ **Never executed** — a measurement or investigation step itself was planned but never run
>   (distinct from a *candidate* being rejected — here the *question* was never answered).
> - ✅ **Closed since this audit was compiled** — built, or answered by measurement, in a later
>   round. Kept in place (rather than deleted) so the audit stays a complete ledger of what was
>   ever discovered; the row names the document that closed it.
>
> **Round 8 (2026-07-27)** closed a block of these by executing
> [`26-remaining-work-implementation-plan.md`](./26-remaining-work-implementation-plan.md) —
> see [`27-remaining-work-implementation.md`](./27-remaining-work-implementation.md) for the
> record, and note that #3's preflight surfaced a blocker no prior round could have hit.

---

## 1. Performance candidates — GPU / pipeline architecture

| # | Candidate | Status | Ceiling / evidence | Source |
|---|---|---|---|---|
| 1 | **Cross-tile multiprocessing (`workers>1`)** | 🟡→ **Gate satisfied (round 8)**: 2.216x measured on the real full slide, correctness veto passed ([27](./27-remaining-work-implementation.md) §6.5). Ship decision now rests on a **VRAM** caveat, not a speed one — `workers=4` peaked at 30,439 MB of 32,607 (93.3%, ~2.2 GB headroom) | 3.09×–3.51× on tissue-dense crops; 2.06×–2.17× on composition-matched crops | [21-impl](./21-cross-tile-multiprocessing-implementation.md), [19-backlog #1](./19-open-backlog.md) |
| 2 | **`workers≥6` allocator-fragmentation OOM** (one worker balloons to exactly 24.76 GiB) | 🟢→ **Candidate fix now tried (round 8)** — knob built (`config.cuda_alloc_conf`, applied to the env immediately before workers spawn) and swept by `scripts/alloc_conf_probe.py`; see [27](./27-remaining-work-implementation.md) §5 | Reliability defect, not sized as speed | [21 §4.7](./21-cross-tile-multiprocessing-implementation.md), [23 §4.6/§6](./23-next-optimization-cycle-implementation.md), [19-backlog #7b](./19-open-backlog.md) |
| 3 | **Full real-WSI-scale end-to-end validation** | ✅ **DONE (round 8) — CLOSED after being open since doc 09 §3.6.** Both runs completed on the real 27,565-tile slide, correctness veto passed. 3.82 h / 1.73 h vs 2.6 h / 1.25 h projected; speedup 2.216x. The preflight also surfaced a blocker no crop-based round could hit (per-modality canvas sizes). It found **three crop-derived numbers that do not survive at scale** — see [27](./27-remaining-work-implementation.md) §6.4/§6.6 | n/a — the binding gate on shipping item 1 | [19-backlog #7](./19-open-backlog.md), [22 §1](./22-next-optimization-cycle-plan.md) (descoped again in round 6), round 7 corrected two of its inputs but did not run it |
| 4 | **`_stitch_overlay_slide` opens all 27,565 tiles at once; no `RLIMIT_NOFILE` check** | ✅ **CLOSED (round 8)** — `_ensure_nofile_limit()` raises the soft limit itself when the hard limit permits, else fails loudly before opening anything; 7 tests. See [27](./27-remaining-work-implementation.md) §1 | Would have failed on a host with the common 1,024 limit | [25 §5.2](./25-gpu-encode-decode-loop-acceleration-implementation.md), [19-backlog #7](./19-open-backlog.md) |
| 5 | **Phase D slide stitch — cheaper non-GPU knobs** (`tiffsave` tile-size/pyramid-depth params, skip re-encoding constant background regions) | ✅ **CLOSED (round 8), NEGATIVE** — all 13 configs ablated single-knob; every knob named here is dead (tile size monotonically worse, pyramid depth a no-op). `zstd` won 1.2331x but **QuPath/BioFormats cannot open it** → vetoed on correctness. Nothing adopted. See [27](./27-remaining-work-implementation.md) §3 | Stitch measured at 322.7 s / 16.2 GP, superlinear; ceiling 1.036×–1.078× | [25 §5.3, §11](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 6 | **Phase D slide stitch — GPU port (nvImageCodec)** | 🟢 **Reopened by the round-8 full-slide run.** Two constraints from item 5 still hold — output must be **BioFormats-readable** (rules out modern codecs; doc 25 §8.2's 19.2x was for lossless LZW, which is fine), and pyramid + BigTIFF must be assembled by hand. But the **ceiling is ~3x what doc 24 assumed**: the real stitch is **19.3% of wall at `workers=4`** (ceiling **1.239x**), not 7.3%, and took 1,185 s vs the probe's 322.7 s. **Now the largest single remaining lever in the pipeline** ([27](./27-remaining-work-implementation.md) §6.6) | nvImageCodec encodes lossless LZW TIFF 19.2× faster than the pipeline's pyvips call, but produces no pyramid/BigTIFF — real engineering (downsample levels + container assembly) still undesigned | [24 §2.1](./24-gpu-encode-decode-loop-acceleration-plan.md), [25 §8.2](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 7 | **Candidate F — dedup background-tile placeholder writes (`os.link` instead of re-encoding)** | 🔴 Stop-lossed for speed (BG arm has 47–53% slack, zero wall payoff at any worker count) — but flagged as a **separate storage decision** (~157 GB/slide of identical uncompressed TIFF), never decided | 407×/272× cheaper than the current write path, if ever wanted | [25 §4.3, §11](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 8 | **Candidate G — hoist redundant per-call `mkdir()` out of `_save_tile_array`** | 🔴 Built, measured, **reverted** (0.056% of wall; end-to-end ablation reads slightly negative) | Patch preserved verbatim in [25 §10](./25-gpu-encode-decode-loop-acceleration-implementation.md) for one-edit revival if arm balance or storage backend changes (e.g. NFS, where the same call is ~1000× more expensive) | [24 §2.6](./24-gpu-encode-decode-loop-acceleration-plan.md), [25 §3](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 9 | **Candidates B/D/E — `detect_all_dots`, `enlarge_cell_instances`/`build_all_positive_results`, per-tile debug encode → GPU (CuPy/cuCIM)** | 🔴 Stop-lossed twice (doc 22's B2/B3, doc 24/25) — 1.00× wall ceiling at real composition (BG arm has 47–53% slack); also CuPy **cannot run on this host** today (numpy≥2 ABI conflict with project's `numpy<2` pin; the numpy-1 line can't JIT without a CUDA toolkit) | Reopen only if MAIN sheds ~50% first | [22 §3 B2/B3](./22-next-optimization-cycle-plan.md), [24 §2.2–§2.4](./24-gpu-encode-decode-loop-acceleration-plan.md), [25 §2.3, §8.1](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 10 | **`clear_slide_edge_cells` — last CPU glue in the M2→M3b device-idle gap** | 🟢 Open, watch item, not actioned (too small alone, ~1.2% of wall) | Natural next candidate only if bubble-closing work resumes | [19-backlog #2](./19-open-backlog.md), [18 §5 follow-up #5](./18-gpu-starvation-prerequisites-implementation.md) |
| 11 | **CUDA-stream / pipeline-depth-2 bubble redesign** (overlap intra-tile CPU glue with the *next* forward, not the *previous* tile's back end) | 🔴 Sized and stop-lossed (≤1.065× after item ⑧ landed) | Reopen only if intra-forward launch-bound idle is fixed upstream | [17 §3](./17-gpu-starvation-prerequisites-plan.md), [18 §3](./18-gpu-starvation-prerequisites-implementation.md), [19-backlog #4](./19-open-backlog.md) |
| 12 | **CUDA graph capture / vectorize Cellpose's internal kernel-launch Python loops** (`_extend_centers_gpu`, `steps_interp`, `get_masks_torch`, `fill_holes_and_remove_small_masks`) | 🔴 Stop-lossed twice — traced against 4.0.8 (~1.23×) then re-confirmed against 4.2.1.1/`cpdino` (~1.118×, and `get_rel_pos` no longer even applies) | Requires patching pinned third-party `cellpose`/`segment_anything` internals | [gil-contention-diag "追加深挖"](./measurement/gil-contention-diag.md), [19-backlog #5](./19-open-backlog.md), [23 §7](./23-next-optimization-cycle-implementation.md) |
| 13 | **GPU-side tile/transform loading** (move `_read_rgb` decode onto GPU) | 🟢 **REOPENED (round 8)** — stopped out at 1.22% of wall / ceiling 1.012× on a 441-tile crop (round 4), but the real full-slide run measured `B2r_tile_read` at **17.2% of wall** (2,368.5 s) — the ~49 GB precut scratch no longer fits page cache at production scale. Ceiling not yet re-derived | — | [17 §4](./17-gpu-starvation-prerequisites-plan.md), [18 §6.3](./18-gpu-starvation-prerequisites-implementation.md), [19-backlog #6](./19-open-backlog.md), [27 §6.4](./27-remaining-work-implementation.md) |
| 14 | **Isolate the `detect_all_dots` +22.3% regression (round 3, item ⑨)** — numpy/scikit-image/opencv downgrade vs. retrained-checkpoint cell geometry, never separated | 🟢 Open, no wall-clock payoff (ceiling 1.013×; largely overtaken by round 6's `dot_detect_n_jobs=1` fix), cheap to settle: rerun over saved instance masks under both dependency sets | — | [bottleneck-list ⑨](./measurement/bottleneck-list.md), [19-backlog #3](./19-open-backlog.md) |
| 15 | **Multi-request / concurrent-job behavior of the API/job layer** (Phase E) — never measured | ⚪ Never executed — flagged as a risk since [09 §3.5](./09-measurement-analysis-plan.md), still not load-tested; gated on confirming concurrent-slide serving is even part of the deployment plan | — | [19-backlog #8](./19-open-backlog.md), [22 §2 A3 / §1](./22-next-optimization-cycle-plan.md), [23 §1](./23-next-optimization-cycle-implementation.md) |
| 16 | **`pip freeze` next to every measurement round** | 🟡 Partially adopted — round 2 (`_metrics_current/`) has no snapshot and can't be fixed retroactively (it's why item 14 can't be attributed cleanly) | Process discipline, not perf | [19-backlog #9](./19-open-backlog.md) |
| 17 | **CPU-core contention audit under N-way multiprocessing (Track A2)** | 🔴 Executed — hypothesis (over-subscription) was **wrong**; found a bigger, different defect instead (`detect_all_dots`'s joblib fan-out is a net loss at *every* process count, 2.77× slower than serial) | Fixed via `dot_detect_n_jobs=1`, a one-line config change | [22 §2 A2](./22-next-optimization-cycle-plan.md), [23 §4](./23-next-optimization-cycle-implementation.md) |
| 18 | **Cross-tile Cellpose batching** (stack N tiles into one `(N,H,W,3)` `eval()` call, `Lz>1` genuine batching path in `cellpose.core.run_net`) | 🔴 Root cause traced and confirmed real, then measured **negative** — flat-to-worse (≤+8.2%) at every group size, VRAM cost scales linearly (1.17→8.01 GB); re-checked at G=16, still worse (+5.9%/+6.6%) and 15.8 GB (48.6% of the card) | Do not build; `cellpose_batch_size` stays 16 | [22 §3 B1](./22-next-optimization-cycle-plan.md), [23 §2](./23-next-optimization-cycle-implementation.md), [25 §7](./25-gpu-encode-decode-loop-acceleration-implementation.md) |
| 19 | **Cross-tile UNet++ batching**, incl. porting `cell_mask/unet_mask/inference.py`'s `predict_batch` | 🔴 Research lead followed up and found dead — `predict_batch` is a serial per-image loop, not a batched forward, nothing to port; measured strictly worse at every group size anyway | Amdahl-capped at ~0.4% of wall before it started | [22 §3 B1b](./22-next-optimization-cycle-plan.md), [23 §3](./23-next-optimization-cycle-implementation.md) |
| 20 | **CUDA MPS (multi-context GPU sharing)** | 🔴 Tested — genuinely works on this consumer RTX 5090 (+44% on a launch-bound microbenchmark), but **flat end-to-end** (real pipeline no longer serialization-limited at its knee) | — | [20 Candidate C](./20-cross-tile-multiprocessing-plan.md), [21 §5](./21-cross-tile-multiprocessing-implementation.md) |
| 21 | **Candidate A — CPU-back-end-only process pool** (deepen the background thread pool / process pool without touching CUDA) | 🔴 Premise tested via a cheap depth-2 thread knob (strictly cheaper than the full IPC design) and refuted — +2.8% *slower* single-process, flat under multiprocessing | Knob built, measured, deleted | [20 Candidate A](./20-cross-tile-multiprocessing-plan.md), [21 §6](./21-cross-tile-multiprocessing-implementation.md) |
| 22 | **Candidate E — fork-based model reuse across worker processes** | 🔴 Explicitly "do not build" — architecturally unsafe (CUDA contexts are not fork-safe), recorded only to close the door on it being re-tried | — | [20 Candidate E](./20-cross-tile-multiprocessing-plan.md) |
| 23 | **`gc.collect` batching (fixed-N cadence)** | 🔴 Built, measured, added nothing beyond `gc.freeze()` alone, **deleted** | `gc.freeze()` shipped instead | [16-gc-collect-frequency-result](./16-gc-collect-frequency-result.md) |
| 24 | **`detect_all_dots` → process-safe joblib backend (`loky`/spawn)** | 🔴 Diagnosed via py-spy and judged not worth pursuing — GIL competition from the background thread was *not* the driver of GPU idle (idle came from the main thread's own Python overhead); the spawn-safety/memmap validation scripts this would have required were **never written**, because the decision tree never reached that branch | Superseded entirely by round 6's unrelated `dot_detect_n_jobs=1` finding | [11 §4(b)](./11-gpu-pipeline-stage2-plan.md), [gil-contention-diag](./measurement/gil-contention-diag.md), [12 §3(b)](./12-detect-all-dots-optimization-plan.md) |
| 25 | **`detect_all_dots` — `regionprops_table` vectorized rewrite (§3(c) of doc 12)** | ⚪ Never executed — was doc 12's fallback if (b) above turned out not worth it; the entire branch was made moot by round 6's `dot_detect_n_jobs=1` discovery before anyone returned to run doc 12's own "Step 0" re-measurement | — | [12 §2, §3(c)](./12-detect-all-dots-optimization-plan.md) |
| 26 | **`detect_all_dots` — whole-tile vectorization (no per-cell Python loop at all, §3(d) of doc 12)** | 🟢 Open in principle (listed explicitly as backlog, highest ceiling, highest correctness risk) — never picked up; the cheaper `dot_detect_n_jobs=1` fix addressed the wall-clock problem via a completely different mechanism, so this was never revisited | — | [12 §3(d)](./12-detect-all-dots-optimization-plan.md) |
| 27 | **`torch.compile` on the UNet++ inferencer** | ⚪ Never executed — back-pocket idea only, Amdahl-capped at ~0.4% of wall before it starts | — | [22 §4](./22-next-optimization-cycle-plan.md) |
| 28 | **CUDA graph capture around the UNet++ call** | ⚪ Never executed — same ~0.4% cap as #27 | — | [22 §4](./22-next-optimization-cycle-plan.md) |
| 29 | **NVIDIA DALI / TensorRT for UNet++ or Cellpose forward** | ⚪ Never executed — not sized, flagged only, would add fp16/int8 correctness risk | — | [22 §4](./22-next-optimization-cycle-plan.md) |
| 30 | **A1 — re-tune `workers` count against *real* full-WSI tissue density** (as opposed to a crop) | 🟡 Partially done — re-swept on crops in round 6/7 (recommendation moved 6→4–5, then 4 confirmed again at composition-matched scale), but the *real full-WSI density* half of the question is still open, tied to item 3 | — | [22 §2 A1](./22-next-optimization-cycle-plan.md), [23 §6](./23-next-optimization-cycle-implementation.md), [19-backlog #7](./19-open-backlog.md) |
| 31 | **Cellpose 4.0.8 SAM-backbone kernel targets superseded** — any future attempt on cellpose internals should now target `_from_device` (`cellpose/core.py:141`, 24.2% of MAIN-arm Python samples) and `_quantile` (10.1%), not the four functions item 12/#12-above lists | 🟢 Open observation, no attempt made yet (correctly so — ceiling is small and it's third-party internals) | — | [23 §7](./23-next-optimization-cycle-implementation.md) |
| 43 | **`gc.freeze()`'s crop-scale win (~0 cost) does not survive to full-slide scale — reverts to 16.1% of wall** | 🟢 **Open, discovered round 8, highest-value remaining item** — `run_batch` accumulates `per_tile_owned` (356,255 `CellAnalysisResult` dataclasses by the end of a slide), created *after* the freeze, fully tracked, rescanned on every one of 27,565 `gc.collect()` calls. Invisible on a 441-tile crop (~6,000 objects). Plausible fix (freeze again periodically, or keep accumulating results out of GC's reach) not yet built | — | [16-gc-collect-frequency-result.md](./16-gc-collect-frequency-result.md), [19-backlog #6b](./19-open-backlog.md), [27 §6.4](./27-remaining-work-implementation.md) |
| 44 | **`B2r_tile_read`'s crop-scale stop-loss (1.22%) does not survive to full-slide scale — 17.2% of wall** | 🟢 **Reopened round 8** — the ~49 GB precut scratch no longer fits page cache at production scale, so reads that were free on a crop become real disk I/O | — | [18 §6.3](./18-gpu-starvation-prerequisites-implementation.md), [19-backlog #6](./19-open-backlog.md), [27 §6.4](./27-remaining-work-implementation.md) |

## 2. Documentation ↔ code drift, discovered but not fixed

| # | Item | Status | Source |
|---|---|---|---|
| 32 | **`generate_ihc_core_mask` parameter named `ihc_tile_path: Path` but always receives an ndarray** | ✅ **CLOSED (round 8)** — renamed to `ihc_image: Union[np.ndarray, Path, str]`; the now-redundant `# pyright: ignore` at the call site removed too ([27](./27-remaining-work-implementation.md) §7) | [07 G5](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |
| 33 | **`docs/sdd-elastic-dish-matching.md` referenced in `m3_elastic_matching.py`'s docstring but doesn't exist** | ✅ **CLOSED (round 8)** — stripped; a **second, unlisted instance in `m3_dot_detection.py`** was found and stripped too ([27](./27-remaining-work-implementation.md) §7) | [07 G4](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |
| 34 | **`docs/dish_dot_detection_spec.md` referenced in `config.py`/`config_example.py` comments but doesn't exist** | ✅ **CLOSED (round 8)** — stripped from both `config.py` and `config_example.py` ([27](./27-remaining-work-implementation.md) §7) | [07 G4](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |
| 35 | **`docs/algo/elastic_matching_v3_explainer.html` describes the wrong (nucleus-centric) matching algorithm** — current code is cell-centric + overlap-priority + reach | ✅ **CLOSED (round 8)** — updated to v4: Part A rewritten cell-centric, the obsolete '多核排除' outcome removed, and the parameter table corrected (it listed two params that do not exist and struck through `dish_elastic_expand_factor`, which the code uses on every call) ([27](./27-remaining-work-implementation.md) §7.1) | [04 "為什麼某些嘗試被棄用"](./04-optimization-roadmap.md), [07 G4](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |
| 36 | **No automated test guards `config.py`/`config_example.py` parity** | ✅ **CLOSED (round 8)** — `tests/test_config_parity.py`, 6 cases over field names/order/types/non-site-local defaults + the module tail G2 lost ([27](./27-remaining-work-implementation.md) §7.2) | [07 G2](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |
| 37 | **`backend/algorithms/hybrid/` has zero automated pipeline-correctness tests** — the only automated check is `scripts/verify_gc_freeze.py`, a standalone invariant guard for one optimization, not a general suite. `m0_stitch` (pure data reorg, no model) flagged as the best candidate for a synthetic-numpy unit test | ✅ **CLOSED (round 8)** — `tests/test_m0_stitch.py`, 21 cases, incl. a full-slide coverage assertion that core crops partition the slide exactly once ([27](./27-remaining-work-implementation.md) §7.3) | [05-dev-testing-guide "現有測試方式"](./05-dev-testing-guide.md), [19-backlog §3](./19-open-backlog.md) |
| 38 | **Codegraph phantom-file list never reconfirmed against the current `backend/algorithms/hybrid/` path** (G1's list was recorded against the old `cell_mask/hybrid/` path before the directory moved) | ✅ **CLOSED (round 8)** — rechecked against the current path: **zero phantoms** (20 indexed, 0 indexed-but-absent); the one unindexed `.py` is the gitignored `config.py`, which is correct behaviour, not staleness. See [27](./27-remaining-work-implementation.md) §7.4 | [07 G1](./07-gotchas-appendix.md), [19-backlog §3](./19-open-backlog.md) |

## 3. Correctness / clinical validation — discovered, blocking, never resolved

| # | Item | Status | Source |
|---|---|---|---|
| 39 | **Round-3 Cellpose checkpoint retrain (Cellpose 4.0.8→4.2.1.1 `cpdino` swap) needs pathologist/clinical sign-off** | 🟢 Open, pending since round 3, unresolved through round 7 — cell counts shifted +1.8–2.5% and one tile flipped success→skipped between round 2 and round 3; every performance win from round 3 onward rides on top of this unvalidated model swap | [13 §3](./13-next-optimization-plan.md), [bottleneck-list-history "Round 3" correctness note](./measurement/bottleneck-list-history.md), [19-backlog §2](./19-open-backlog.md), [23 §9](./23-next-optimization-cycle-implementation.md) |

## 4. Measurement questions raised but never answered

| # | Item | Status | Source |
|---|---|---|---|
| 40 | **Per-bucket timing inside multiprocess workers does not exist** — `perf_measure.py`'s instrumentation is parent-process-only, so any future *intra-worker* optimization at `workers>1` is currently unmeasurable | ✅ **CLOSED (round 8)** — built exactly as identified, via an env-gated probe hook so the pipeline keeps no dependency on the measurement harness; 4 parent-only buckets → 26 worker-side buckets ([27](./27-remaining-work-implementation.md) §4) | [21 §10 follow-up #1](./21-cross-tile-multiprocessing-implementation.md) |
| 41 | **Persistent worker-pool design for the API server path** — today's `workers>1` design spawns fresh processes per `run_batch` call (paying `~3.14s × N` init per request if ever enabled for the API); a persistent pool would need the `gc.freeze()`/`unfreeze` contract re-designed per-call, never done | 🟡 Blocked on item 15 (multi-request behavior) being confirmed as a deployment requirement first | [20 §5](./20-cross-tile-multiprocessing-plan.md), [21 §10 follow-up #2](./21-cross-tile-multiprocessing-implementation.md) |
| 42 | **No partial-resume/checkpointing for `run_batch`** — fail-fast discards the *whole* batch on any single-tile failure; for a full WSI, an OOM at tile 30,000 of 27,565+ costs the entire run | ✅ **CLOSED (round 8)** — `run_batch(checkpoint=True)`, opt-in, config_hash-guarded, fail-fast unchanged; cold vs resumed output byte-identical ([27](./27-remaining-work-implementation.md) §2) | [21 §10 follow-up #7](./21-cross-tile-multiprocessing-implementation.md) |

---

## What this audit does *not* include

- Items already shipped and adopted (`gc.freeze()`, precut streaming, ⑧ moved off MAIN, `cellpose_batch_size` wiring, `dot_detect_n_jobs=1`, cross-tile multiprocessing's core machinery) — these **did** change the code; see `measurement/bottleneck-list.md` and `measurement/current-status-comparison.md` for what landed and its measured effect.
- Pure Discover/Analyze/planning documents that produced no candidates of their own (08, 09) — their job was to scope the next document, and they succeeded; nothing in them was "discovered and dropped."
- The playbook docs themselves (`PERFORMANCE_BOTTLENECK_PLAYBOOK.md` / `.quickref.md`) — methodology, not a task list.
