# frontend — React + Vite (TS)

## rules

1. @../.claude/rules/frontend-backend-boundary.md

**Short version:** you may edit `frontend/**`, `backend/api/**`,
`backend/schemas/**`. Every other backend file is read-only — if one has to
change, stop coding and warn the user first. The required stop report is in
the rule above.
